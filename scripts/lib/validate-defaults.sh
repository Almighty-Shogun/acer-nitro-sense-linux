SAMPLES="${SAMPLES:-10}"
INTERVAL="${INTERVAL:-2}"
OUT_DIR="${OUT_DIR:-/tmp/acer-nitro-sense-validation-$(date +%Y%m%d-%H%M%S)}"
LOAD_CMD="${LOAD_CMD:-}"
LOAD_WORKERS="${LOAD_WORKERS:-4}"
COOLDOWN_SECONDS="${COOLDOWN_SECONDS:-20}"
RESTORE_COOLDOWN_SECONDS="${RESTORE_COOLDOWN_SECONDS:-45}"
MODE_MATRIX="${MODE_MATRIX:-0}"
DAILY_MODE="${DAILY_MODE:-0}"
CALIBRATE_FANS="${CALIBRATE_FANS:-0}"
CALIBRATION_TARGETS="${CALIBRATION_TARGETS:-all}"
CALIBRATION_SPEEDS="${CALIBRATION_SPEEDS:-10 15 20 25 30 35 40 45 50 60 75 90 100}"
CALIBRATION_FAN_MODE="${CALIBRATION_FAN_MODE:-manual}"
CALIBRATION_PROFILE="${CALIBRATION_PROFILE:-quiet}"
CALIBRATION_SETTLE_SECONDS="${CALIBRATION_SETTLE_SECONDS:-8}"
CALIBRATION_SAMPLES="${CALIBRATION_SAMPLES:-3}"
EC_DUMP="${EC_DUMP:-0}"
EC_DUMP_START="${EC_DUMP_START:-0x10}"
EC_DUMP_END="${EC_DUMP_END:-0x60}"
FAN_MODE_SCAN="${FAN_MODE_SCAN:-0}"
FAN_MODE_SCAN_MODES="${FAN_MODE_SCAN_MODES:-auto manual turbo}"
FAN_MODE_SCAN_PROFILES="${FAN_MODE_SCAN_PROFILES:-quiet balanced performance}"
FAN_MODE_SCAN_SETTLE_SECONDS="${FAN_MODE_SCAN_SETTLE_SECONDS:-8}"
SAFETY_VALIDATE="${SAFETY_VALIDATE:-0}"
SAFETY_VALIDATE_MODES="${SAFETY_VALIDATE_MODES:-manual preset auto firmware-auto}"
SAFETY_VALIDATE_PERCENT="${SAFETY_VALIDATE_PERCENT:-25}"
SAFETY_VALIDATE_PRESET="${SAFETY_VALIDATE_PRESET:-quiet}"
SAFETY_VALIDATE_SETTLE_SECONDS="${SAFETY_VALIDATE_SETTLE_SECONDS:-8}"
ANS="${ANS:-ans}"
VALIDATION_STARTED_AT="$(date --iso-8601=seconds 2>/dev/null || date)"
LOAD_PIDS=""
